# Ideal Instruction — static design preview

A **static snapshot** of the in-progress WordPress rebuild (developed locally, fully isolated
from the live site), for visual review only. Dynamic features (member login, registration,
form submission, gated video playback) are not functional on a static host and work only on the
real WordPress build. Imagery is placeholder; `noindex` is set so this preview is not indexed.
